apiVersion: v2
name: engine
description: LLM inference server with a paged KV cache, continuous batching and an OpenAI-compatible API
type: application
version: 0.1.0
appVersion: "v0.0-cpu"
home: https://github.com/AarnavNoble/llm-engine
sources:
  - https://github.com/AarnavNoble/llm-engine
